apiVersion: v1 
kind: Service
metadata:
  name: nginx
spec:
  type: LoadBalancer
  selector:
    project: expense
    module: backend
    environment: dev
  ports:
  - name: nginx-svc-port
    port: 80
    protocol: TCP
    targetPort: 80